import { ChangeiconDirective } from './changeicon.directive';

describe('ChangeiconDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeiconDirective();
    expect(directive).toBeTruthy();
  });
});
