import { ChangetypepassDirective } from './changetypepass.directive';

describe('ChangetypepassDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangetypepassDirective();
    expect(directive).toBeTruthy();
  });
});
